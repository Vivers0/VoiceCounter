{
  "name": "voicecounter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "build": "coffee --compile --output src/build/ src/lib/",
    "start": "node src/build/index"
  },
  "author": "Vivers",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^11.5.1"
  },
  "devDependencies": {
    "coffeescript": "^2.5.1"
  }
}
